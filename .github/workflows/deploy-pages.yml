name: Deploy documentation site

on:
  push:
    branches: [master]
    paths:
      - site/**
      - EditingSystem/**
      - .github/workflows/deploy-pages.yml
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up .NET
        uses: actions/setup-dotnet@v4

      - name: Install Lunet
        run: dotnet tool install --global lunet --version 1.0.16

      - name: Build site
        working-directory: site
        run: ~/.dotnet/tools/lunet build

      - name: Install WebAssembly build tools
        run: dotnet workload install wasm-tools

      - name: Publish Avalonia browser demo
        run: |
          dotnet publish EditingSystem/Jewelry.EditingSystem.Avalonia.Demo.Browser/Jewelry.EditingSystem.Avalonia.Demo.Browser.csproj --configuration Release --output .artifacts/avalonia-demo
          mkdir -p site/.lunet/build/www/demo
          cp -R .artifacts/avalonia-demo/wwwroot/. site/.lunet/build/www/demo/

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site/.lunet/build/www

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
