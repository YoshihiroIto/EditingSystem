<UserControl x:Class="Jewelry.EditingSystem.Avalonia.Demo.MainView"
        xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="using:Jewelry.EditingSystem.Avalonia.Demo"
        MinWidth="900"
        MinHeight="600">
    <UserControl.DataContext>
        <local:MainWindowViewModel />
    </UserControl.DataContext>

    <UserControl.Styles>
        <Style Selector="Border.resize-handle">
            <Setter Property="Width" Value="10" />
            <Setter Property="Height" Value="10" />
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#2563EB" />
            <Setter Property="BorderThickness" Value="2" />
            <Setter Property="CornerRadius" Value="2" />
        </Style>
        <Style Selector="Border.resize-handle:pointerover">
            <Setter Property="Background" Value="#DBEAFE" />
        </Style>
    </UserControl.Styles>

    <Grid RowDefinitions="Auto,*"
          Background="{DynamicResource SystemControlBackgroundAltHighBrush}">
        <Border Grid.Row="0"
                Padding="10"
                BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
                BorderThickness="0,0,0,1">
            <Grid ColumnDefinitions="Auto,18,Auto,*,Auto"
                  VerticalAlignment="Center">
                <StackPanel Grid.Column="0"
                            Orientation="Horizontal"
                            Spacing="8">
                    <Button Command="{Binding AddObjectCommand}"
                            Content="Add" />
                    <Button Command="{Binding DeleteSelectedCommand}"
                            Content="Delete"
                            IsEnabled="{Binding HasSelection}" />
                    <Button Command="{Binding AlignLeftCommand}"
                            Margin="24,0,0,0"
                            Content="Align Left"
                            IsEnabled="{Binding HasMultipleSelection}" />
                    <Button Command="{Binding AlignTopCommand}"
                            Content="Align Top"
                            IsEnabled="{Binding HasMultipleSelection}" />
                    <Button Command="{Binding BringSelectedToFrontCommand}"
                            Content="To Front"
                            IsEnabled="{Binding HasSelection}" />
                    <Button Command="{Binding SendSelectedToBackCommand}"
                            Content="To Back"
                            IsEnabled="{Binding HasSelection}" />
                </StackPanel>

                <StackPanel Grid.Column="2"
                            Margin="16,0,0,0"
                            Orientation="Horizontal"
                            Spacing="6">
                    <Button Command="{Binding UndoCommand}"
                            Content="Undo"
                            IsEnabled="{Binding History.CanUndo}" />
                    <TextBlock VerticalAlignment="Center"
                               Text="{Binding History.UndoCount}" />
                    <Button Margin="8,0,0,0"
                            Command="{Binding RedoCommand}"
                            Content="Redo"
                            IsEnabled="{Binding History.CanRedo}" />
                    <TextBlock VerticalAlignment="Center"
                               Text="{Binding History.RedoCount}" />
                </StackPanel>

                <StackPanel Grid.Column="4"
                            Orientation="Horizontal"
                            Spacing="8">
                    <Button x:Name="MarkSavedButton"
                            Command="{Binding MarkSavedCommand}"
                            Content="Mark Saved"
                            IsEnabled="{Binding History.IsDirty}" />
                    <TextBlock x:Name="SaveStateText"
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"
                               Text="{Binding SaveStateText}" />
                </StackPanel>
            </Grid>
        </Border>

        <Grid ColumnDefinitions="*,400"
              Grid.Row="1">
            <Border Grid.Column="0"
                    Margin="12"
                    BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
                    BorderThickness="1"
                    CornerRadius="6">
                <Grid x:Name="EditorSurface"
                      Background="#20252B"
                      ClipToBounds="True"
                      PointerPressed="EditorSurface_PointerPressed">
                    <ItemsControl x:Name="ObjectItems"
                                  Margin="0"
                                  ItemsSource="{Binding Objects}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <Canvas />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>

                        <ItemsControl.Styles>
                            <Style Selector="ContentPresenter">
                                <Setter Property="Canvas.Left" Value="{Binding X}" />
                                <Setter Property="Canvas.Top" Value="{Binding Y}" />
                            </Style>
                        </ItemsControl.Styles>

                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="local:DemoObject">
                                <Grid Width="{Binding Width}"
                                      Height="{Binding Height}">
                                    <Border Background="{Binding Brush}"
                                            CornerRadius="4"
                                            Opacity="{Binding Opacity}"
                                            BorderThickness="1"
                                            BorderBrush="#121212"
                                            PointerMoved="Object_PointerMoved"
                                            PointerPressed="Object_PointerPressed"
                                            PointerReleased="Object_PointerReleased">
                                        <TextBlock HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"
                                                   FontWeight="SemiBold"
                                                   Foreground="White"
                                                   IsHitTestVisible="False"
                                                   Text="{Binding Name}" />
                                    </Border>

                                    <Border BorderBrush="#60A5FA"
                                            BorderThickness="2"
                                            CornerRadius="4"
                                            IsHitTestVisible="False"
                                            IsVisible="{Binding IsSelected}" />

                                    <Border Classes="resize-handle"
                                            HorizontalAlignment="Left"
                                            VerticalAlignment="Top"
                                            Cursor="TopLeftCorner"
                                            IsVisible="{Binding IsSelected}"
                                            PointerMoved="ResizeHandle_PointerMoved"
                                            PointerPressed="ResizeHandle_PointerPressed"
                                            PointerReleased="ResizeHandle_PointerReleased"
                                            Tag="NorthWest" />
                                    <Border Classes="resize-handle"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Top"
                                            Cursor="SizeNorthSouth"
                                            IsVisible="{Binding IsSelected}"
                                            PointerMoved="ResizeHandle_PointerMoved"
                                            PointerPressed="ResizeHandle_PointerPressed"
                                            PointerReleased="ResizeHandle_PointerReleased"
                                            Tag="North" />
                                    <Border Classes="resize-handle"
                                            HorizontalAlignment="Right"
                                            VerticalAlignment="Top"
                                            Cursor="TopRightCorner"
                                            IsVisible="{Binding IsSelected}"
                                            PointerMoved="ResizeHandle_PointerMoved"
                                            PointerPressed="ResizeHandle_PointerPressed"
                                            PointerReleased="ResizeHandle_PointerReleased"
                                            Tag="NorthEast" />
                                    <Border Classes="resize-handle"
                                            HorizontalAlignment="Right"
                                            VerticalAlignment="Center"
                                            Cursor="SizeWestEast"
                                            IsVisible="{Binding IsSelected}"
                                            PointerMoved="ResizeHandle_PointerMoved"
                                            PointerPressed="ResizeHandle_PointerPressed"
                                            PointerReleased="ResizeHandle_PointerReleased"
                                            Tag="East" />
                                    <Border Classes="resize-handle"
                                            HorizontalAlignment="Right"
                                            VerticalAlignment="Bottom"
                                            Cursor="BottomRightCorner"
                                            IsVisible="{Binding IsSelected}"
                                            PointerMoved="ResizeHandle_PointerMoved"
                                            PointerPressed="ResizeHandle_PointerPressed"
                                            PointerReleased="ResizeHandle_PointerReleased"
                                            Tag="SouthEast" />
                                    <Border Classes="resize-handle"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Bottom"
                                            Cursor="SizeNorthSouth"
                                            IsVisible="{Binding IsSelected}"
                                            PointerMoved="ResizeHandle_PointerMoved"
                                            PointerPressed="ResizeHandle_PointerPressed"
                                            PointerReleased="ResizeHandle_PointerReleased"
                                            Tag="South" />
                                    <Border Classes="resize-handle"
                                            HorizontalAlignment="Left"
                                            VerticalAlignment="Bottom"
                                            Cursor="BottomLeftCorner"
                                            IsVisible="{Binding IsSelected}"
                                            PointerMoved="ResizeHandle_PointerMoved"
                                            PointerPressed="ResizeHandle_PointerPressed"
                                            PointerReleased="ResizeHandle_PointerReleased"
                                            Tag="SouthWest" />
                                    <Border Classes="resize-handle"
                                            HorizontalAlignment="Left"
                                            VerticalAlignment="Center"
                                            Cursor="SizeWestEast"
                                            IsVisible="{Binding IsSelected}"
                                            PointerMoved="ResizeHandle_PointerMoved"
                                            PointerPressed="ResizeHandle_PointerPressed"
                                            PointerReleased="ResizeHandle_PointerReleased"
                                            Tag="West" />
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <Border Margin="12"
                            Padding="8,5"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Top"
                            Background="#AA000000"
                            CornerRadius="4"
                            IsHitTestVisible="False">
                        <TextBlock FontSize="12"
                                   Foreground="White"
                                   Text="Click: select   Ctrl+Click: multi-select   Drag: move selection" />
                    </Border>

                    <Border Margin="12"
                            Padding="8,5"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Bottom"
                            Background="#AA000000"
                            CornerRadius="4"
                            IsHitTestVisible="False">
                        <TextBlock FontSize="12"
                                   Foreground="White"
                                   Text="Move / Resize / Opacity / Color = CoalescingBatch   •   Name = normal Undo   •   Z-order / Align / Multi-delete = Batch" />
                    </Border>
                </Grid>
            </Border>

            <Border Grid.Column="1"
                    Margin="0,12,12,12"
                    Padding="16"
                    Background="{DynamicResource SystemControlBackgroundAltHighBrush}"
                    BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
                    BorderThickness="1"
                    CornerRadius="6">
                <Border.Styles>
                    <Style Selector="TextBlock.info">
                        <Setter Property="FontSize" Value="12" />
                        <Setter Property="Foreground" Value="Orange" />
                        <Setter Property="VerticalAlignment" Value="Bottom" />
                    </Style>
                </Border.Styles>

                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock FontSize="20"
                                   FontWeight="SemiBold"
                                   Text="Properties" />

                        <StackPanel Margin="0,8,0,0"
                                    Orientation="Horizontal"
                                    Spacing="5">
                            <TextBlock Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"
                                       Text="Selected:" />
                            <TextBlock FontWeight="SemiBold"
                                       Text="{Binding SelectionCount}" />
                        </StackPanel>

                        <Separator Margin="0,4,0,16" />

                        <StackPanel Margin="0,8,0,0"
                                    Orientation="Horizontal"
                                    Spacing="8">
                            <TextBlock FontWeight="SemiBold"
                                       Text="Name:" />

                            <TextBlock Classes="info"
                                       IsVisible="{Binding HasMultipleSelection}"
                                       Text="Select a single object to rename." />
                        </StackPanel>

                        <TextBox x:Name="NameEditor"
                                 Margin="0,6,0,8"
                                 IsEnabled="{Binding HasSingleSelection}"
                                 PlaceholderText="Select 1 object to rename"
                                 Text="{Binding SelectedObject?.Name,
                                                Mode=TwoWay,
                                                UpdateSourceTrigger=LostFocus}" />

                        <StackPanel Margin="0,8,0,0"
                                    Orientation="Horizontal"
                                    Spacing="8">
                            <TextBlock FontWeight="SemiBold"
                                       Text="Opacity:" />
                            <TextBlock Classes="info"
                                       IsVisible="{Binding HasMixedOpacity}"
                                       Text="Mixed values" />
                        </StackPanel>

                        <DockPanel HorizontalSpacing="8">
                            <TextBlock DockPanel.Dock="Right"
                                       VerticalAlignment="Center"
                                       Text="{Binding SelectionOpacity,
                                                      StringFormat='{}{0:F2}'}" />
                            <Slider x:Name="OpacityEditor"
                                    Maximum="1"
                                    Minimum="0"
                                    IsEnabled="{Binding HasSelection}"
                                    Value="{Binding SelectionOpacity,
                                                    Mode=TwoWay}" />
                        </DockPanel>

                        <StackPanel Margin="0,8,0,8"
                                    Orientation="Horizontal"
                                    Spacing="8">
                            <TextBlock FontWeight="SemiBold"
                                       Text="Color:" />
                            <TextBlock Classes="info"
                                       IsVisible="{Binding HasMixedColor}"
                                       Text="Mixed values" />
                        </StackPanel>

                        <ColorView x:Name="ColorEditor"
                                   IsAlphaEnabled="False"
                                   IsAlphaVisible="False"
                                   IsColorComponentsVisible="True"
                                   IsColorPaletteVisible="True"
                                   IsColorSpectrumVisible="True"
                                   IsEnabled="{Binding HasSelection}"
                                   Color="{Binding SelectionColor,
                                                   Mode=TwoWay}" />

                        <Separator Margin="0,12,0,4" />

                        <TextBlock FontWeight="SemiBold"
                                   Text="How to try it" />
                        <TextBlock Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"
                                   Text="Edit an object and the title shows * with Unsaved changes. Mark Saved records the current save point without writing a file. Edit again, then Undo back to that point to become Saved automatically; Redo makes it unsaved again. Select several rectangles with Ctrl+Click and drag one to move the whole selection in a single undo entry. Resize handles, Opacity, and ColorView demonstrate coalesced continuous edits."
                                   TextWrapping="Wrap" />
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>
